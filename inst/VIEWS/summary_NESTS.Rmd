---
title: ""
geometry: "left=0.8cm,right=0.5cm,top=1cm,bottom=1cm"
fontsize: 11pt
output:
  pdf_document
---

```{r setup, include=FALSE}
require(wadeR)
require(xtable)
options(xtable.comment = FALSE)
```

###Reference date: `r Sys.Date()`

```{r nests, echo=FALSE, message=FALSE, warning=FALSE,results='asis'}
n = NESTS()[species == 'REPH']
n = n[, .(nest, nest_state, lastCheck, iniClutch, clutch, IN, m_sure, f_sure, msr_state)]
CA = c('R201', 'R901', 'R902', 'R1201', 'R602', 'R603', 'R801')
n[nest %in% CA, CA := CA]
n[!is.na(CA), CA := 'CA']
n[nest %like% 'REPH', Rick := TRUE]
n = n[is.na(Rick)]
n[, Rick := NULL]

x = xtable(n, auto = TRUE, caption = NULL, floating = TRUE, floating.environment = "sidewaystable", latex.environments = "" )

print(x, hline.after = 0:nrow(x))

```






